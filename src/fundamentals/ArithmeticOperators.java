package fundamentals;

public class ArithmeticOperators {
    public static void main(String[] args) {
        // operatory matematyczne
        int a = 5;
        System.out.println("a=" + a);

        int b = 10;
        System.out.println("b=" + b);

        int c= a + b;  //dodawanie
        System.out.println("a+b=" + c);

        c = a - b; // odejmowanie
        System.out.println("a-b=" + c);

        c = a * b;  // mnożenie
        System.out.println("a*b=" + c);

        c = b/a;  //dzielenie
        System.out.println("b/a=" + c);

        c= b%a;  // modulo (reszta z dzielenia)
        System.out.println("b%a=" + c);

        /*  c +=
            c -=
            c *=
            c /=

            Operator += - do wartości istniejącej zmiennej dodaje podaną wartość i wynik automatycznie przypisuje do tej zmiennej, np.:
            int a = 50;
            a += 50;    // zmienna po dodanu będzie miała wartość 100

            Operator -= - analogiczne działanie jak operatora +=, tylko zamiast operacji dodawania wykonywana jest operacja odejmowania, np.:
            int a = 50;
            a -= 40;    // zmienna po wykonaniu operacji odejmowania będzie miała wartość 10

            Operator *= - analogiczne działanie jak wyżej, tylko zamiast operacji + oraz - wykonywane jest mnożenie, np.:
            int a = 10;
            a *= 10;    // zmienna po wykonaniu operacji mnożenia będzie miała wartość 100

            Operator /= - analogiczne działanie jak wyżej, tylko zamiast operacji +, - czy też * wykonywane jest dzielenie, np.:
            int a = 200;
            a /= 100;    // zmienna po wykonaniu operacji dzielenia będzie miała wartość 2

         */


    }




}
